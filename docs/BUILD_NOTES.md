# Build notes

## Local development

Install the project's dependencies before starting development, then use the repository's existing development command.

## Verification

Before submitting a change, run the available lint, type-check, and production-build commands. UI changes should also be checked at the supported viewport sizes.

Keep generated build output out of source control unless the repository explicitly requires it.
