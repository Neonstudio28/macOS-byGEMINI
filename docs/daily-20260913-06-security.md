# External content boundary

Treat remote content and user-provided strings as untrusted input. Keep rendering paths constrained, avoid injecting raw HTML unnecessarily, and never expose local credentials through UI or diagnostic output.