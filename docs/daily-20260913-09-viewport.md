# Viewport QA

Exercise narrow, wide, portrait, and resized windows. Important controls and status content must remain reachable and readable after a resize without relying on a fixed viewport assumption.